machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t dakue/influxdb .

test:
  override:
    - docker run -d -p 8086:8086 dakue/influxdb; sleep 30
    - curl --retry 10 --retry-delay 5 -v http://localhost:8086
